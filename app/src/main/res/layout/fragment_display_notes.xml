<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="ui.list.DisplayNotesFragment">

    <data>

        <import type="android.view.View" />

        <import type="ru.gross.notes.ui.list.NoteView" />

        <import type="java.util.List" />

        <import type="ru.gross.notes.data.common.State" />

        <variable
            name="state"
            type="State" />

        <variable
            name="notes"
            type="List&lt;NoteView>" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/notes_presenter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="@dimen/bottom_appbar_height"
            android:visibility="@{state.isLoading() ? View.GONE: View.VISIBLE, default=gone}"
            app:layoutManager="androidx.recyclerview.widget.StaggeredGridLayoutManager"
            app:spanCount="2"
            bind:itemsSource="@{notes}"
            tools:itemCount="13"
            tools:listitem="@layout/note_item_layout"
            tools:visibility="visible" />

        <include
            layout="@layout/view_state_layout"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="@+id/notes_presenter"
            app:layout_constraintEnd_toEndOf="@+id/notes_presenter"
            app:layout_constraintStart_toStartOf="@+id/notes_presenter"
            app:layout_constraintTop_toTopOf="@+id/notes_presenter"
            bind:state="@{state}"
            tools:visibility="gone" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>